# 职达简历 —— 全面优化与重构实施方案

| 项目 | 内容 |
| --- | --- |
| 审计日期 | 2026-07-26 |
| 审计对象 | `C:\Users\Administrator\Desktop\简历生成器`（FastAPI 单体 + 原生 Web + Android Compose） |
| 审计方式 | 全量源码静态审计（后端 354KB / 前端 400KB / Android Kotlin 全量），关键结论已逐条回查代码行 |
| 审计范围限制 | 服务器 115.120.206.64 因沙箱环境不可用，**运行时层（Nginx、systemd、真实数据量、日志、资源占用）尚未核查**，见第 11 章 |
| 交付范围 | Web（PC + 移动）+ Android App 同步重构，云端与本地源码同步 |

---

## 1. 结论摘要

职达简历已经是一个**功能闭环完整**的产品：注册登录、简历库、JD 解析、AI 改写、Word/PDF 双导出、岗位雷达、职业中心、额度计费、管理后台、Android 原生端全部打通。这一点必须先肯定——功能广度远超多数同阶段产品。

问题不在功能，而在三个层面的**工程地基**：

**第一层：正确性地基有洞。** 额度系统（`reserve`/`commit`/`release`）、用户注册索引、短信频控三处都是"读—改—写"且没有任何锁或事务。单用户测试永远看不到问题，两个并发请求就能触发额度双花或同名账号双写。文件下载接口用 `in` 做路径归属判断，可以被路径穿越绕过。这些是上线前必须堵死的。

**第二层：可信度地基是空的。** 产品的核心承诺是"忠于事实、禁止虚构"，但代码里这个承诺**完全由 prompt 承担**，AI 输出后没有任何校验——没有数字/日期比对，没有原文 diff 展示，`career_facts`（用户已确认/否认的事实库）建好了却从未接入生成链路，用户否认过的经历照样能被改写进简历。质量评分里"AI 痕迹"这一项是硬编码的"通过"（`services.py:558`）。生成进度的 `progress_steps` 写死在创建时（`main.py:2311`/`2419`）之后再不更新，前端读到的永远是第一步。这些不是 bug，是**信任透支**。

**第三层：可维护性地基在崩。** `main.py` 3065 行、`services.py` 88KB、`app.js` 2240 行单 IIFE、`styles.css` 115KB 含两套并存的登录样式，零后端测试、零 CI、零崩溃上报。Android 的 release 包用 debug keystore 签名（`build.gradle.kts:26-33`），密码 `"android"` 明文写在源码里——这个包上不了任何应用商店。

**总体定级：** 功能成熟度 B，工程成熟度 D+。当前架构可承载约 300 注册 / 50 日活 / 5 RPS；超过这个量级，额度竞态将高概率触发，对象存储的全量列举成本会失控。

**优化主线（本方案的组织逻辑）：**

> 先堵正确性的洞（P0，1 周）→ 再补可信度（P1，1 个月）→ 同步重构前端与 App（1-2 个月）→ 最后升级推荐与工程体系（2-3 个月）

---

## 2. P0 阻塞清单（第 1 周必须完成）

这 6 项在修复前不应继续扩大用户规模。

### P0-1 额度双花：`reserve`/`commit`/`release` 无原子性

**位置：** `app/main.py:592-642`

**证据：** `reserve_credits` 的执行序列是 `billing_account()` 读 → 内存改 `account["reserved"] += amount` → `store.put_json()` 覆盖写，中间无锁、无版本号、无事务。`commit_credits`（613）与 `release_credits`（630）同构。

**攻击/故障路径：** 用户余额 1 次，同时发两个 `POST /generations`。两个请求各自读到 `reserved=0`、`credits=1`，各自通过 `available_credits >= 1` 校验，各自写 `reserved=1`。结果两次生成都被受理，但只冻结了 1 次额度。反向的并发 `release` 会让 `reserved` 少减，账面额度虚增。

**修法（按落地成本排序）：**

1. **立即可上线（1 天）：** 引入按用户维度的进程内 `asyncio.Lock` + 乐观并发校验。给 account 增加 `version` 字段，`put_json` 前重新读取并比对 `version`，不一致则重试最多 3 次后返回 409。这只在单进程部署下完全有效，与当前 uvicorn 单 worker 部署一致。
2. **正式方案（1 周）：** 项目已有 `MetadataStore` + `settings.database_url`（PostgreSQL）通道。将 `billing/accounts` 与 `billing/ledger` 迁到 PG 真表，`reserve` 改为单条 SQL：

```sql
UPDATE billing_accounts
   SET reserved = reserved + %(amount)s, updated_at = now()
 WHERE user_id = %(uid)s
   AND suspended = false
   AND credits - reserved >= %(amount)s
RETURNING credits, reserved;
```

零行返回即余额不足，直接抛 402。整个操作在一条语句内原子完成，彻底消除竞态。`commit`/`release` 同理改写。

3. **配套：** ledger 增加 `UNIQUE(user_id, kind, reference_id)` 约束，让重复入账在数据库层被拒绝。

### P0-2 注册竞态：用户名/手机号索引 check-then-act

**位置：** `app/main.py:1276-1300`

**证据：** `if get_user_by_username(...)` → `if get_user_by_phone(...)` → `save_user(user)`，三步非原子。`save_user`（158）写 user 对象 + 写两个索引 key，最后写入者覆盖索引。

**后果：** 同名并发注册产生两条 user 记录、两份密码哈希，索引只指向其中一条，另一条成为**带有效密码但永久不可登录**的孤儿数据。手机号同理，会导致短信登录指向错误账号。

**修法：** 迁 PG 后加 `UNIQUE (lower(username))` 与 `UNIQUE (phone)`，捕获 `UniqueViolation` 返回 409。过渡期用条件写或注册串行锁。

### P0-3 文件越权：路径归属用 `in` 判断可被穿越绕过

**位置：** `app/main.py:2571-2576`

**证据：**

```python
if user.get("role") != "admin" and f"users/{user['id']}/" not in key:
    raise HTTPException(status_code=403, ...)
```

`key` 是 `{key:path}` 路径参数，完全用户可控。构造 `users/<自己ID>/../../users/<受害者ID>/resume.docx`，子串检查通过，随后 `store.presigned_url(bucket, key)` 直接签出他人文件的下载链接。简历文件含姓名、手机号、完整工作经历——这是可直接导致数据泄露事件的漏洞。

**修法：**

```python
import posixpath
normalized = posixpath.normpath(key).lstrip("/")
if ".." in normalized.split("/"):
    raise HTTPException(400, "非法路径")
prefix = f"users/{user['id']}/"
if user.get("role") != "admin" and not normalized.startswith(prefix):
    raise HTTPException(403, "无权访问该文件")
```

并把后续所有 `store.*` 调用统一改用 `normalized`。更彻底的做法是取消这个通用下载入口，全部改走已有的 `file_token` 短时签名机制。

### P0-4 Android release 包用 debug keystore 签名

**位置：** `android/app/build.gradle.kts:25-40`

**证据：** `storeFile = ~/.android/debug.keystore`，`storePassword = "android"`，`keyAlias = "androiddebugkey"`，且 `buildTypes.release` 确实引用了这个 `signingConfig`。

**后果：** ① 无法上架 Google Play / 国内任一应用商店；② debug 证书是公开固定证书，任何人都能用同一证书打出"合法升级包"覆盖安装用户的 App，配合 P0-5 的自更新通道即构成完整的供应链劫持链条。

**修法：**

1. 本地生成正式 keystore，**不入库**，`android/keystore.properties` 加进 `.gitignore`；
2. `build.gradle.kts` 改为从 `keystore.properties` 或环境变量读取，读不到时 release 构建直接 `throw GradleException`，绝不静默降级到 debug 证书；
3. keystore 与密码离线备份两份（丢失即无法再发布升级包）。

### P0-5 APK 自更新缺签名指纹校验

**位置：** `android/app/src/main/java/com/zhiday/resume/MainViewModel.kt:591-727`（校验逻辑在 613-617）

**证据：** 下载 APK 后校验了 `packageName` 与 `versionCode`，但**未校验签名指纹**。下载地址由后端 `/app/version` 返回（`main.py:1211-1240`，且 URL 与版本号硬编码在源码里）。

**后果：** 后端被攻破或 HTTP 链路被中间人（当前正是明文 HTTP，见 P0-6）即可下发任意 APK，App 会主动请求安装（已申请 `REQUEST_INSTALL_PACKAGES`）。

**修法：** 安装前用 `PackageManager.getPackageArchiveInfo(path, GET_SIGNING_CERTIFICATES)` 取出待装包签名，与当前运行包的签名 SHA-256 比对，不一致直接删除文件并上报。同时把 `latest_code`/`latest_name`/下载域名从 `main.py` 挪到 `settings`，改版本不再需要改代码。

### P0-6 全链路明文 HTTP

**位置：** `android/app/build.gradle.kts:16`（`API_BASE_URL = "http://115.120.206.64/api/"`）、`AndroidManifest.xml:11`（`usesCleartextTraffic="true"` 全局放开）、`res/xml/network_security_config.xml`

**证据：** 主 API 走 IP + HTTP；`API_FALLBACK_URL` 已经是 `https://zhidajob.top/api/`，说明 HTTPS 域名已就绪，只是主通道没切过去。

**后果：** JWT、密码、短信验证码、完整简历内容在公共网络明文传输，可被同网段嗅探或运营商侧劫持。

**修法：** 主备通道互换（HTTPS 域名为主，IP 为备且仅在域名解析失败时使用）；移除 Manifest 上的全局 `usesCleartextTraffic`，仅在 NSC 中对备用 IP 精确豁免；服务端加 HSTS。同时修 `_resolve_cookie_secure`（`main.py:418-428`）——它读 `request.url.scheme`，在 Nginx 终止 TLS 后转发 HTTP 的场景下会误判，应优先读 `X-Forwarded-Proto`。

---

## 3. 后端架构重构

### 3.1 拆分单体 `main.py`

**现状：** 3065 行一个文件，装了认证、计费、简历、生成、雷达、管理后台、后台任务处理函数，路由直接调 `store.get_json`/`put_json`，无 Service 层无 Repository 层。`store`/`ai`/`sms` 是模块级全局变量（`main.py:94-98`），路由靠闭包引用，`app.dependency_overrides` 无法替换，导致**根本无法写单元测试**。

**目标结构：**

```
app/
├── main.py                 # 只留 app 装配、lifespan、中间件、异常处理（目标 < 200 行）
├── deps.py                 # get_store / get_ai / get_sms / current_user / admin_user
├── api/
│   ├── auth.py             # 注册/登录/短信/改密/注销
│   ├── billing.py          # 额度/订单/账本
│   ├── resumes.py          # 简历 CRUD/上传/解析
│   ├── jd.py               # JD 提交与解析
│   ├── generations.py      # 生成任务与结果
│   ├── radar.py            # 岗位雷达
│   ├── career.py           # 职业中心/事实库/复盘/投递
│   ├── files.py            # 文件下载与令牌
│   └── admin.py            # 管理后台
├── services/               # 业务逻辑，纯函数优先，不感知 HTTP
├── repositories/           # 数据访问，唯一允许触碰 store/DB 的层
├── ai/                     # LLM 客户端 + prompt 仓库
└── prompts/                # YAML prompt 模板（带 version）
```

**拆分纪律（这是重构成功的关键，必须写进 CLAUDE.md）：**

- `api/` 层只做参数校验、调 service、组装响应，**不允许出现 `store.`**；
- `services/` 层不允许 `import fastapi`、不允许抛 `HTTPException`（抛领域异常，由 api 层翻译）；
- `repositories/` 层是唯一能碰 `store`/`psycopg` 的地方；
- 所有依赖通过 `Annotated[X, Depends(get_x)]` 注入，禁止新增模块级全局单例。

**执行方式：** 按领域逐个搬迁，每搬完一个领域先补该领域的契约测试再进下一个，全程保持 `git` 可回滚。**不做一次性大爆炸重写。**

### 3.2 数据层：把 TOS 对象存储从"数据库"位置上撤下来

**现状问题（三条互相叠加）：**

`app/storage.py:137-160` 的 `list_json` 实现是「1 次 LIST + N 次 GET」。而 `career_dashboard`（512）、`admin_stats`（2938-2949）、`admin_operations`（2851-2873）、`radar_recommendations`（986/1010/1016）都在调它。`admin_stats` 一次请求扫 `users/` + `resumes/` + `generations/` 三个前缀全量——1000 用户 × 10 次生成，单次点击产生约 1 万次对象存储请求。

`app/metadata_store.py:41-57` 每次 `get_json`/`put_json` 都 `psycopg.connect()` 建新连接，无连接池，并发下会打满 PG 的 `max_connections`。

对象存储不提供事务、条件写和唯一约束，这是第 2 章三个 P0 竞态的**共同根因**。

**目标分工：**

| 数据类型 | 归属 | 理由 |
| --- | --- | --- |
| 用户、额度账户、账本、订单、任务、投递、事实库 | **PostgreSQL 真表** | 需要事务、唯一约束、聚合查询 |
| 岗位库、反馈、曝光、语义缓存 | PostgreSQL（从 SQLite 迁出） | 解除 SQLite 写锁瓶颈 |
| 原始简历、生成的 Word/PDF | TOS-B（保持） | 大文件，对象存储是正确选择 |
| 简历/JD 截图、头像 | TOS-C（保持） | 同上 |

**改造要点：**

- `metadata_store.py` 换成 `psycopg_pool.ConnectionPool`（min=2, max=10），彻底移除每次建连；
- 建表时把当前的 key-value JSON 存法升级为真表 + 必要索引（`users.username`、`users.phone`、`generations(user_id, created_at desc)`、`applications(user_id)`）；
- `storage.py` 里 `bucket_usage()` 仍在走全量列举（205-238），改为读 PG 中维护的用量计数器；
- 所有列表接口加 `limit`/`cursor` 分页（现在 `/generations`、`/resumes`、`/applications`、`/reviews`、`/career/facts` 全是全量返回）；
- 管理端统计改 `COUNT`/`SUM` 聚合 SQL，并加 60 秒缓存。

### 3.3 消除事件循环阻塞

**现状：** `async def` 路由里直接调同步 boto3/psycopg。已确认的点包括 `generate_resume`（`main.py:2258` 起）、`radar_recommendations`（`main.py:986`、`1010`、`1016`）。这些同步网络调用在 event loop 线程内执行，会**卡住整个进程**的所有并发请求。

**修法：** 短期把所有 `async def` 路由内的 `store.*` 调用包成 `await asyncio.to_thread(...)`（`services.py:98` 已有正确范例）；中期 repository 层统一改异步驱动。加一条 CI 静态检查：`async def` 函数体内出现裸 `store.` 调用即失败。

### 3.4 任务队列：从"重启即丢"到可靠投递

**现状：** `config.py:79` 的 `task_queue_enabled` 默认 `False`，此时 `dispatch_task`（`main.py:193-203`）退化成 Starlette `BackgroundTasks`——进程内执行，重启/崩溃即任务全丢，且用户额度已被 `reserve` 冻结，永不释放。

**更严重的配套缺陷：** 没有任何超时清理机制。`status=processing` 的生成任务会永久卡住，前端一直转圈，冻结额度永不归还，用户账户可用额度被逐次蚕食到 0。

**修法：**

1. 生产/预发环境强制 `TASK_QUEUE_ENABLED=true`（`config.py:121` 已有生产校验，需覆盖 staging）；
2. 新增 reaper 定时任务（RQ Scheduler 或 APScheduler，每 5 分钟）：扫 `status=processing` 且 `updated_at` 超 10 分钟的 generation / jd-task，标记 `failed` 并调 `release_credits`；
3. 实现幂等：`main.py:284` 的 CORS 已经声明允许 `Idempotency-Key` 请求头，但**服务端从未读取它**。在 `generate_resume` / `parse_jd` 中读该头，以 `idempotency/{user_id}/{key}` 存首次结果，重复请求直接返回原结果，避免网络抖动导致双倍扣费。

### 3.5 接口与可观测性

| 项 | 现状 | 改法 |
| --- | --- | --- |
| API 版本 | 无版本段（`main.py:287`，直接挂 `/api/`） | 改 `/api/v1/`，旧路径保留 6 个月 301 |
| 裸 dict 入参 | `radar_save_preset`（`main.py:1096`）等接受 `payload: dict`，零校验 | 全部补 Pydantic 模型 + `max_length` |
| URL 长度 | `JDRequest.url` 无长度上限（`models.py:107-110`） | `Field(max_length=2048)` |
| 日志 | `observability.py:65-67` 拼字符串，无结构化 | 换 `structlog` JSON 输出 |
| trace 传播 | 中间件有 `request_id`，后台任务丢失 | task 创建时带上 `trace_id` 并贯穿 |
| 指标 | 无 `/metrics`，无告警 | `prometheus-fastapi-instrumentator` + 队列积压/LLM 失败率/渲染降级率三条告警 |
| 崩溃/异常 | 无 Sentry | 后端与 Android 同时接入 |
| CSP | `main.py:330` 含 `unsafe-inline` | 前端构建化后改 nonce |
| 管理后台 | 仅靠 JWT 里的 `role`（`main.py:453-456`） | 加 TOTP 二次验证 + IP 白名单 |
| `public_user` | 每次调用都签一次 file_token，`admin_users` 列 100 人签 100 次 JWT | 只在 `/auth/me` 签；列表返回 `avatar_key` |

---

## 4. AI 能力与「忠于事实」的可信度建设

这是本方案里**对产品竞争力影响最大**的一章。

### 4.1 补上后置事实核查（P0 级产品缺陷）

**现状：** `ai.py:277-298` 的 `rewrite_resume` 返回后，`main.py:2153` 直接 `normalize_resume_content` 然后写入结果。从 LLM 输出到用户看到简历之间，**没有任何一行校验代码**。"禁止编造"这个核心承诺 100% 依赖模型自律，而 LLM 在缺少数据时补一句"效率提升 30%"是高频行为，用户无从察觉。

**修法（三层递进）：**

**第一层——数字与实体白名单校验（1 周可上线）。** 从原始简历抽取所有数字、百分比、金额、年份、公司名、学校名、职位名，构成事实白名单。对 AI 输出做同样抽取，任何**新出现且不在白名单**的数字或实体，标记为 `hallucination_suspect`，写入 `ai_score.checks` 并在前端红色高亮，附"此处数据未在原简历中找到，请确认"。

**第二层——原文 diff 可视化（2 周）。** 逐条经历做 `difflib` 比对，前端以"原文 / 改写后"双栏 + 词级高亮呈现。这是同类标杆产品的标配，也是用户建立信任的关键界面。

**第三层——事实库回流（1 周）。** `career_facts`（`main.py:664-707`）已经建好，用户可以确认/否认事实条目，但 `process_generation_job` 里**从未读取它**。改为：调 `rewrite_resume` 前先拉该用户 `status=denied` 的条目，从传入的 `resume["content"]` 中剔除；`status=confirmed` 的条目作为强锚点写进 prompt。

### 4.2 修掉三处"假反馈"

用户能感知到的虚假信号，比功能缺失更伤信任。

| 问题 | 位置 | 现状 | 修法 |
| --- | --- | --- | --- |
| 进度条永远停在第一步 | `main.py:2311`、`2419` 写入静态 5 步，`process_generation_job` 全程不回写；`app.js:1187` 照原样渲染 | 用户以为卡住了 | 每个阶段（解析→改写→评分→渲染→上传）完成即回写对应 step 状态与时间戳 |
| "AI 痕迹"检查恒为通过 | `services.py:558` 硬编码 `"status": "通过"` | 虚假质量背书 | 实现套话词频检测（赋能/驱动/全面负责/大幅提升/深度参与…），超阈值标"需审查" |
| 评分有地板效应 | `services.py:543` `content_quality = min(95, 62 + ...)`，空简历也得 62 分，综合分下限约 38 | 所有简历都"良好"，评分失去意义 | 空经历/空技能时让 `content_quality` 落到 20-30 区间，让分布反映真实质量 |

### 4.3 LLM 调用工程化

| 项 | 现状 | 改法 |
| --- | --- | --- |
| prompt 管理 | 全部硬编码字符串（`ai.py:159/211/277/309`），改 prompt = 改代码 + 发版 | 抽到 `prompts/*.yaml`，带 `version` 字段，支持灰度与回滚 |
| 退避抖动 | `ai.py:129` 是 `sleep(min(2**attempt, 4))`，无 jitter（文档声称有，实现没有） | `sleep(min(2**attempt, 8) * random.uniform(0.7, 1.3))`，避免 429 后齐步重试 |
| JSON 解析容错 | `ai.py:113-119` 重试时只压缩上下文，未加强格式约束 | 二次重试时注入 schema few-shot；对支持的模型开 `response_format=json_object` |
| 多图 JD 串行 | `ai.py:218` 列表推导内 `await`，10 张图 = 10 次串行，最差 ~150s | `asyncio.gather` + `Semaphore(5)` |
| 成本可见性 | `ai.py:67-69` 只记调用次数，不记 token | 记 `response.usage` 的 prompt/completion tokens，按模型和用户维度累计 |
| 长等待无反馈 | 全部非流式，`max_tokens=8000` + 55s 超时 | 改写环节开 SSE 流式，前端逐段渲染 |
| 图片 OCR 无超时 | `main.py:1734` 无 `wait_for`（文档路径 1673 有） | 补 `asyncio.wait_for(timeout=50)` + 降级提示 |

### 4.4 解析与导出

**解析入口收窄的问题：** `.doc` 直接抛错（`services.py:41`），扫描版 PDF 提取不到文字就抛 422 让用户"改用截图 OCR"（`main.py:1669-1670`）。这两类用户（制造业/国企/政府背景候选人占比不低）在上传第一步就被劝退。修法：`.doc` 走 LibreOffice headless 转换；PDF 文本为空时自动 `pdf2image` 转图后走已有的 `structure_resume(image_url=...)` OCR 路径，把 OCR 负担从用户手里接回来。

**导出双引擎不一致：** Word 导出把字体硬编码为 `Microsoft YaHei`/微软雅黑（`services.py:881`、`1035-1036`），Linux 服务器上必然缺失，LibreOffice 打开会降级成文泉驿；而 PDF 走 Playwright + 内置 `SourceHanSansCN`，两条路径视觉完全不同。reportlab 降级路径（`services.py:1198-1215`）又搜的是系统字体，是第三套字体。修法：三条路径统一使用项目内置的思源字体文件；DOCX 侧考虑嵌入字体或改走 `pandoc`。

**Playwright 冷启动：** `services.py:1802-1815` 每次生成都 `chromium.launch()`，冷启动 1-3 秒且吃内存。修法：在 `lifespan` 中启动固定 2-3 个 browser 实例做池，生成时借 `page` 用完归还。

**渲染降级静默：** `main.py:2162-2165` 捕获异常后只把 `design["render_engine"]` 改成 `reportlab_fallback`，用户拿到样式差很多的 PDF 却不知道，运营也看不到降级率。修法：在 `ai_score.checks` 增加"渲染引擎"项，并 `log_event("render_fallback", ...)` 接入告警。

---

## 5. 岗位采集与推荐系统升级

### 5.1 采集层

**当前实际能力：** 只有两个采集器——`GxrcPublicCollector`（广西人才网，`radar_sources.py:63-78`）和 `JsonFeedCollector`（通用 JSON feed，306 起）。但 `radar.py:999-1005` 的 `facets()` 里**硬编码返回了 51job、猎聘、BOSS 直聘、智联**四个平台作为可选筛选项。用户勾选后必然零结果——这是明确的功能欺骗，优先级高于任何技术优化。

| 问题 | 位置 | 修法 |
| --- | --- | --- |
| 假数据源选项 | `radar.py:999-1005` | sources 改为从 `jobs.source_url` 动态派生；未接入的平台不展示或标"即将上线" |
| 无采集调度 | `deploy.sh` 只装 service，无 timer/cron | 加 systemd timer（`OnCalendar=*:0/4`）或 RQ Scheduler 周期任务 |
| 无请求间隔 | `radar_sources.py:63-78` 连发 20 页 POST，无 sleep；详情抓取（176）同样 | 每页后 `sleep(random 0.5~1.5s)`；详情抓取改后台任务逐条 + 重试 |
| UA 不一致 | 列表用 `ZhidaJobRadar/1.0`（95），详情用 Chrome UA（171） | 统一，且在 UA 中留联系方式（善意爬虫惯例） |
| 合规未校验 | 详情页抓 `m.gxrc.com` HTML，非官方 API，无 robots 检查 | 启动时校验 robots.txt；`JsonFeedCollector` 加来源白名单与授权登记 |
| 无失败告警 | `radar_health_check.py` 只打 JSON 到 stdout | 注册 systemd timer（30 分钟），非零退出触发飞书/钉钉 webhook |
| 导入无上限 | `main.py:1189-1195` 接受任意长度 list，`replace=True` 先全表 `is_active=0` | 单次上限 5000，超出拆异步分批 |

### 5.2 数据质量

**薪资解析（已复核，实际 bug 与初判不同）：** `radar.py:84-96` 的 `_salary_min` 用 `re.search(r"(\d[\d,]*)\s*[kK]?", text)` 只取第一个数字。实测："10-20K" → 10000（正确）；"8000-15000元/月" → 8000（正确）；但 **"1.5万-2万" → 1**（小数与"万"单位完全未处理）；**"15万/年" → 15**；"面议" → 0 且无日志。含"万"或年薪表述的岗位，薪资排序和筛选会给出严重错误的结果。

修法：显式解析区间与单位——

```python
_SALARY_RE = re.compile(r"(\d+(?:\.\d+)?)\s*(?:[-~至到]\s*(\d+(?:\.\d+)?))?\s*(万|k|K|千)?")
```

按 `万`×10000、`k/千`×1000 换算，识别"/年"再 ÷12，全部归一到"元/月"，无法解析时返回 `None` 而非 0（0 会混进排序），并记一条 warn 日志便于发现新格式。

**其余数据质量项：**

- 经验/学历筛选是字符串 `in` 匹配（`radar.py:801-806`），用户选"3-5年"匹配不到"三至五年"或"3年以上"。改法：`normalize_job()` 时映射出数值字段 `experience_years_min`，筛选走数值区间。
- 去重只看 `source_url`（`radar.py:312-319`、`radar_sources.py:287-296`），招聘方重发即产生重复卡片。改法：加 `(归一化title, 归一化company)` 指纹软去重。
- 过期清理有 10% 安全阀（`radar.py:387`），若 90% 岗位都过期则 `guard_blocked` 触发、**一条都不清**（401 行直接 return 0），用户看到满屏失效岗位。改法：加 `force` 参数或按增量同步结果动态调整阈值。

### 5.3 推荐逻辑

**当前本质：** SQLite 全表扫描（`radar.py:721` 固定 `LIMIT 15000`）+ 本地同义词词典关键词匹配 + 加权规则打分，LLM 语义分只覆盖前 15 条（`radar.py:879`）作为装饰层。这是"带权重的布尔搜索"，不是推荐系统。

| 问题 | 位置 | 影响 | 修法 |
| --- | --- | --- | --- |
| 每次请求全量扫描 + Python 侧逐行分词打分 | `radar.py:721` | 5000 岗位 ~200-500ms，1 万条超 1s | SQLite FTS5 倒排索引（中期）→ 向量 ANN（远期） |
| 实时最多 15 次串行 LLM 调用 | `radar.py:879-893` | 冷用户首屏可能等 15-45s | `asyncio.gather` + `Semaphore(5)`；仅第 1 页触发；翻页用关键词分 |
| 语义缓存永不过期 | `radar.py:846-860` 只写 `created_at`，读时无过期判断 | 岗位内容更新后旧分数继续参与排序 | 加 `expires_at = created_at + 7d`；`initialize()` 内清理 7 天前记录 |
| 画像为空时全部返回 50 分 | `radar.py:781` | 冷启动退化为时间排序，却伪装成"匹配度" | 冷启动显式走"最新/热门"策略并在 UI 标注，不假装个性化 |
| 翻页漂移 | `main.py:1020` 每页重新全量打分，`1046` 再切片 | 新岗位入库即导致第 1 页末尾重复出现在第 2 页 | 首次请求缓存有序 job_id 列表（TTL 5min），翻页按 offset 切该列表 |
| 负反馈不回流 | `radar.py:18-19`、`728`：`not_interested` 仅 `continue` 过滤 | 用户反复拒绝销售岗，新销售岗照推 | 从 not_interested 岗位提取高频 tag 构成 `negative_tags`，命中即 -10~-20 分 |
| 公司屏蔽精确匹配 | `radar.py:707-709` `cp.company = j.company` | 屏蔽"腾讯科技（深圳）有限公司"，"腾讯科技"照出 | 存归一化公司名（去法律后缀），按归一化字段匹配 |
| 画像文本含噪声 | `main.py:870-882` 把经历 dict 的所有 `.values()` 扁平 join，日期"2020-01"被当 token | 稀释真实技能词权重 | 改结构化模板，过滤纯数字/日期 token |
| 画像重复计算 | `main.py:1022` 与 `1038` 各调一次 `radar_profile_text`，每次都全量读简历 + 生成记录 | 每请求 2× I/O 放大 | 提前算一次传给两处 |
| 曝光埋点闲置 | `recommendation_impressions` 全库只有建表（257）、写入（830）、按用户删除（1096），**零 SELECT** | 存了数据但算不出 CTR，无法 A/B | 加 `GET /admin/radar/stats`，算 曝光→查看→收藏→投递 漏斗 |
| impression 同步写阻塞 | `radar.py:827-839` executemany 与导入争 SQLite 写锁，timeout=8s | 同步岗位期间用户请求可能超时 | 改 BackgroundTask 或计数异步 flush |
| 语义缓存表无上限 | `radar.py:290` `(profile_hash, job_id)` 主键，100 用户 × 5000 岗位 = 50 万行 | 库膨胀 | 定期清理 + 行数上限 |

**演进路径：**

- **推荐 1.5（1 个月）：** FTS5 索引替代 LIKE 全扫；分页 session 缓存消除漂移；结构化画像；负反馈降权；漏斗埋点上线；语义缓存加 TTL 与并发。
- **推荐 2.0（2-3 个月）：** 岗位与画像做 embedding，存 sqlite-vec 或 PG pgvector，召回从关键词换成 ANN；基于 saved/applied 行为做 item-item 协同过滤；扩展合法数据源。
- **推荐 3.0（3-6 个月）：** 迁 PG 解除 SQLite 并发瓶颈；轻量在线学习（LR/FTRL）实时调权重；标准 A/B 框架衡量 CTR 与召回。

---

## 6. Web 前端重构（PC + 移动同步）

### 6.1 架构：从 2240 行 IIFE 到模块化

**现状：** `app.js` 整体裹在一个 IIFE 里，无 import/export、无构建、无压缩。25+ 字段的全局 `state` 由各 `load*`/`render*` 函数直接赋值，`showApp` 并发发 5 个请求可能相互覆盖状态。约 50 处 `innerHTML` 字符串拼接（`escapeHtml` 用得还算自觉，但这种模式在后续迭代中漏一次就是 XSS）。定时器管理混乱：`logout` 手动 clear 三个 timer，但 `navigate()` 切页不清 poll，反复切页会叠加多个 4s 递归 `setTimeout`；`button.smsTimer` 挂在 DOM 元素上，元素被 `innerHTML` 刷掉后 interval 永不停止。

**目标：** 引入 Vite + ES modules，按页面切分（`radar.js` / `match.js` / `history.js` / `settings.js` / `billing.js`），抽出共享层 `state.js`（统一 `setState` 写入口）、`request.js`（统一 fetch + AbortController + 错误处理）、`dom.js`（`createElement` + `textContent` 构建工具，替换 innerHTML 拼接）、`empty-state.js`（消除 10+ 处重复样板）。所有定时器登记进 `state.timers`，`navigate()` 前统一取消。

是否引入框架：**建议引入 Preact + htm**（3KB，无需 JSX 编译链，可与现有代码渐进共存），而不是整体重写成 React/Vue。理由是现有 DOM 结构和 CSS 可以保留，迁移可以按页面一个个换，风险可控。

### 6.2 响应式与版面

**断点现状：** 15 个 `@media` 全是 `max-width` 桌面优先，断点值有 980/760/720/680 四个，其中 680-760 区间三个值语义重叠；最后几块（`styles.css:1422-1451`）是压缩成一行的补丁式写法，与上方风格明显割裂。`DESIGN.md` 只声明了 680/980 两个断点，实现多出来两个。

**改法：** 收敛为 3 个断点（`< 680px` 移动、`680-1024px` 紧凑、`> 1024px` 桌面），移动端规则改 `min-width` mobile-first 写法，删除 720/760 重叠块。

**PC 端具体问题：** `.page { max-width: 1240px }`（`styles.css:121`）在 1600px+ 屏幕上减去 240px 侧栏后内容区可达 1360px，文字行长远超 75ch 可读上限；`.match-layout` 双列各约 650px，JD textarea 被拉得过宽。改法：内容区 `max-width: 1200px; margin: 0 auto`，纯文字区域再收到 `72ch`。

**移动端具体问题：** iOS safe-area 只处理了侧栏 `padding-bottom`（792）和 sticky filter（839），`settings-dialog` / `preview-dialog` 底部操作区没有 `env(safe-area-inset-bottom)`，iPhone 上按钮被 home indicator 压住；横屏时 `safe-area-inset-left/right` 完全未处理。雷达筛选栏展开后是 2 列共 8 个 select + 1 个 fieldset，375px 屏上高度超 600px 占满全屏且无滚动约束。改法：dialog 补 safe-area；筛选栏改 bottom sheet 抽屉 + `max-height: 80vh; overflow-y: auto`。

### 6.3 设计系统：token 体系已经失效

这是前端最值得先做的一块，因为它同时影响视觉一致性和后续维护成本。

**关键发现：**

- `DESIGN.md` 与 `.impeccable/design.json` 定义主色为 `oklch(49% 0.17 264)`，但 `styles.css:11-13` 实际是 `--accent: #4c6ef5` / `#3b5bdb` / `#e7ecfd` 三个 HEX，暗色模式（1545-1546）同样是 HEX。设计规范与实现从根上就是两套。
- **三个变量被使用但从未声明**：`var(--primary)`（436）、`var(--panel)`（1264，`.review-card` 背景）、`var(--soft)`（1268，`.review-proposal` 背景）。已复核确认 `:root` 与 `[data-theme="dark"]` 中都没有定义。后果是这几个元素背景直接透明——**账单/复盘页现在就是坏的**。
- 硬编码色值散落多处：`.account-avatar` 用紫色三段渐变 + 彩色阴影（343）、`.radar-adapted` 三个 HEX（1302-1304）、`.path-steps li.is-current` 用 `#3b6ef5`（与 `--accent` 的 `#4c6ef5` 是两个不同的蓝）（1412）、`.credits-chip` 两个 HEX（1416）、gap bar 绿色渐变（1474）、`.gap-missing` 自定义红（1476）。
- `.auth-submit` 用 `linear-gradient(...) !important`（1234-1237）——`PRODUCT.md` 的 Anti-references 明确写着"避免渐变文字、玻璃拟态"，`!important` 还会让后续覆盖失效。
- 暗色模式**没有 `prefers-color-scheme` 支持**，只能手动切；更糟的是 `global-theme-i18n.js:115` 默认值是 `"dark"`，而 `ui-preferences.js:9` 默认 `"auto"`，两个脚本同时加载、各写自己的 localStorage key，首屏主题取决于脚本加载顺序。

**改法：** 建立单一 token 源（`tokens.css`，与 `design.json` 用脚本双向校验），把 `--accent` 系列迁到 oklch；补齐 `--primary`/`--panel`/`--soft`；逐个替换上述硬编码色值；删掉渐变与 `!important`；加 `prefers-color-scheme` 监听并统一 `auto` 为唯一默认值。

### 6.4 交互与信息架构

**最该重做的流程是「岗位适配页」。** `index.html:225-239` 里这一页垂直堆了 8 个功能区：选简历 → 选模板 → 选语言 → 提交 JD（3 种方式）→ JD 解析结果 → 亮点补充问卷 → 生成按钮 → 岗位解析历史。移动端单列后长达 6 屏以上，"3. AI 岗位洞察"标题滚出视野后用户完全失去步骤感，生成按钮在最底部且不固定。改法：拆成 4 步 wizard（选简历+模板 → 提交 JD → 确认解析结果+补亮点 → 生成），顶部常驻步骤指示器，移动端生成按钮固定底部。

**生成体验缺闭环。** 提交后立即跳到历史页，用户要自己翻找；无预计剩余时间、无当前步骤明细（`progress_steps` 后端根本不更新，见 4.2）、无取消入口、额度预扣也没有视觉确认。改法：提交后原页保留确认态"已预扣 1 次，后台生成中"；卡片显示真实步骤与预估耗时；提供取消 API；完成时 toast 带"立即预览"跳转。

**其他：** `career`/`applications` 子页被 `navigate()`（`app.js:437-461`）重定向到 `history` 顶级页，子页 URL 不可分享/收藏；`record-tabs` 在 `index.html` 中重复了三份相同 HTML（244-250、253-260、267-273），靠 JS 手动同步 class，极易漏改。表单校验完全依赖 HTML5 `required`，无内联字段级错误提示，密码一致性校验只在后端返回 422 后才提示。

### 6.5 性能

`app.js` 125KB + `styles.css` 115KB 均未压缩未拆包（vendor 的 pdf.js / docx-preview 已是动态 `import()` 懒加载，这部分做得对）。`styles.css:876-1258` 有 380 多行新版登录 showcase 样式，与 45-107 行的旧版并存，实际 `#auth-view` 用的是 React 版动态挂载的那套——约 40KB 是死代码。

**`app.js:747` 传了 `limit: "10000"` 且同时传 `page_size: "20"`**，语义矛盾；后端 `main.py:1017` 确实接受 `limit` 上限 10000。若参数组合导致后端返回大量数据，前端 `renderRadar` 无虚拟化会渲染上万个 DOM 节点。这行应直接删除。

`renderRadar`（`app.js:709-741`）每次筛选都 `list.innerHTML = ...` 全量重绘，滚动位置与焦点全丢。

**改法：** 上 Vite 构建（压缩 + 按页 code splitting + 长效缓存 hash）；删死代码与 `limit: 10000`；长列表接虚拟滚动或严格分页；列表渲染改 keyed diff 保留滚动位置。

### 6.6 可访问性与国际化

`PRODUCT.md` 声称遵循 WCAG 2.1 AA，实际差距：`.skip-link:focus`（43）用 `:focus` 而非 `:focus-visible`，鼠标点击也会弹出 skip link；`input:focus`（87）与按钮的 `:focus-visible` 策略不一致，导致移动端点输入框有蓝框、点按钮没有；`#resume-list`、`#career-facts-list` 等动态列表无 `aria-live`；`#radar-list` 虽有 `aria-live="polite"`，但全量 `innerHTML` 替换会让读屏器重读整个列表；动态生成的分页 input（`app.js:705`）无 label/aria-label，样式还是内联的 `border: 1px solid #ccc`（不响应暗色模式）；dialog 关闭后焦点落到 `<body>` 而非触发按钮；PDF canvas 逐页渲染与 `fitWordPreview` 的 transform 是 JS 控制的，不受 CSS `prefers-reduced-motion` 约束。

**i18n 是两套并存。** `ui-preferences.js` 一份字典 + `data-i18n` 属性替换，`global-theme-i18n.js:10-55` 另一份更小的字典 + 文本节点遍历，两者 localStorage key 不同（`zhida_global_lang` vs `zhiday_ui_prefs`）、主题默认值不同、key 不完全重合。英文覆盖约 110 个 key，基本只覆盖登录页；工作台内 60+ 处中文文案（如 `app.js:472` "还没有职业事实"、`:508` "还没有投递记录"）直接写死在 render 函数里。产品支持输出英文简历，但英文模式下工作台本身大半还是中文。

**改法：** 合并为单一 i18n 模块，统一 storage key 与默认值，把 render 函数里的硬编码文案全部抽 key，英文覆盖率做到 100%；上述 a11y 问题逐条修，并把 axe-core 检查加进 CI。

---

## 7. Android App

**定性纠正：** App **不是 WebView 套壳**，是原生 Kotlin + Jetpack Compose（`MainActivity.kt` 1720 行），功能覆盖认证、简历库、雷达、适配生成、职业中心、账号，与 Web 端基本等价。仅 `DocumentPreviewActivity` 用了一个受控 WebView 加载服务端 `app-document-preview.html` 做文档预览。架构（单 ViewModel + StateFlow + OkHttp + 手写 JSONObject 解析）简单清晰。

**结论：继续深化原生路线，不建议切 Flutter/RN。** 迁移成本远高于在现有基础上修缮，且现有手写解析不依赖反射，混淆安全性反而更好。

**P0 修复（已在第 2 章展开）：** release 签名换正式 keystore；APK 自更新加签名指纹校验；主通道切 HTTPS。

**其余问题：**

| 项 | 位置 | 现状 | 修法 |
| --- | --- | --- | --- |
| WebView 加固 | `DocumentPreviewActivity.kt:21-43` | 开了 JS，未显式关 `allowFileAccess`/`allowContentAccess`/`allowUniversalAccessFromFileURLs`，未设 `mixedContentMode` | 全部显式关闭；设 `MIXED_CONTENT_NEVER_ALLOW` |
| 预览 URL 无校验 | `MainViewModel.kt:491-494` → `DocumentPreviewActivity.kt:31-32` | fileUrl 未做 scheme/域名白名单，理论上可注入 `javascript:` | 校验 `http(s)://` 且域名在白名单内 |
| 无加载/错误态 | `DocumentPreviewActivity.kt:41` 用默认 `WebViewClient()` | 加载失败白屏无提示 | 覆写 `onPageFinished`/`onReceivedError`，加 loading 与重试 |
| allowBackup | `AndroidManifest.xml:6` `true` | SharedPreferences `zhiday_resume` 存 JWT（`ApiClient.kt:178,190-195`），可被 adb backup 导出 | 改 `false` 或用 BackupAgent 排除 token |
| 启动白屏 | `styles.xml:3` 无 `windowBackground` | 冷启动白屏 | `androidx.core:core-splashscreen` |
| 返回键 | 全项目无 `BackHandler` | 子页/表单展开时按返回直接退出 App | 在 `showEditor`、对话框状态注册 `BackHandler` |
| edge-to-edge | `styles.xml:5-8` 硬编码 statusBar/navBar 颜色 | Android 15 强制 edge-to-edge，硬编码会失效 | `enableEdgeToEdge()` + `WindowInsetsController` |
| 键盘遮挡 | 无 `windowSoftInputMode` | 底部输入框可能被键盘压住 | 配 `WindowInsets.ime` |
| 无障碍 | 交互元素只有 `.clickable`，无 semantics | TalkBack 体验很差 | 补 `semantics { role = Role.Button; contentDescription = ... }` |
| 图标 | 导航 tab 用 Unicode 汉字（"简""雷""岗"） | 低 DPI 下模糊，不专业 | 换矢量图标 |
| 死代码 | `MainActivity.kt:1415` 的 `BottomNav` 未被引用（实际用 111 行的 `WorkspaceBottomBar`） | 混淆维护者 | 删除 |
| 混淆规则 | 只有默认 `proguard-android-optimize.txt`，无项目 `proguard-rules.pro` | OkHttp/Coil 若无 consumer rules 可能崩 | 加显式 keep 规则并做 release 冒烟测试 |
| 备份文件入库 | `ApiClient.kt.bak-P03-20260721220654`、`MainViewModel.kt.bak-P03-20260721220654` | 源码历史泄露风险 | 删除并加 `.gitignore` 规则 `*.bak-*` |
| 版本号 | `build.gradle.kts:15-16` 手改两处 | 易漏改 | CI 自动递增，从 git tag 派生 |
| 崩溃/埋点 | 均无 | 线上质量完全盲飞 | 接 Crashlytics 或 Sentry + 关键漏斗埋点 |
| CI | 无 workflow，README 自述"本机未装 Java/Gradle/Android SDK，暂未打出 APK" | 从未验证过 release 构建 | GitHub Actions：`assembleRelease` + lint + 签名 + artifact |
| iOS | 无 `ios/` 目录 | — | 若要做，评估 KMP 复用 ViewModel 层 |

---

## 8. 工程体系建设

**测试（当前为零）。** `pyproject.toml:2` 配了 `testpaths = ["tests"]`，但 `tests/` 目录不存在。补测顺序按风险排：① 额度并发测试（用 `asyncio.gather` 并发 20 次 reserve，断言最终 reserved 精确等于成功次数）；② 注册并发唯一性测试；③ 文件越权测试（路径穿越 payload 必须 403）；④ 幻觉检测器单测（构造含新增数字的 AI 输出，断言被标记）；⑤ 各领域 API 契约测试。目标：核心链路行覆盖 ≥ 70%。

**CI（当前为零）。** 三条流水线：后端（ruff + mypy + pytest + 「async 路由内禁止裸 store. 调用」自定义检查）、前端（eslint + vite build + axe-core a11y 扫描 + 产物体积预算门禁）、Android（assembleRelease + lint + 签名校验）。

**可观测性。** structlog JSON 日志 + trace_id 贯穿后台任务；`/metrics` 暴露 Prometheus 指标；三条核心告警：队列积压 > 50、LLM 失败率 > 5%、PDF 渲染降级率 > 10%。前后端 Android 三端统一接 Sentry。

**配置与密钥。** `config.py` 里 `auth_cookie_secure` 默认 `False`、`admin_password` 默认 `"change-me"`、`task_queue_enabled` 默认 `False`——生产校验（121）只覆盖 `app_env="production"`，staging 会带着这些危险默认值跑。改法：staging 应用生产规则子集；所有危险默认值改为「未配置即启动失败」。

**凭据轮换（重要）。** 服务器 root 密码在本次沟通中以明文出现过。无论本方案是否落地，建议立刻轮换服务器密码、TOS 密钥、SenseNova Key 与 JWT Secret，并把 root 密码登录改为密钥登录 + 禁用密码登录。

---

## 9. 分阶段实施路线图

Web（PC + 移动）与 App 同步推进的节奏安排如下。每个阶段结束都是一个**可发布、可回滚**的状态。

### 阶段 0：止血（第 1 周）

| 端 | 任务 |
| --- | --- |
| 后端 | P0-1 额度原子化（先上乐观锁版本）、P0-2 注册唯一性、P0-3 文件越权修复、任务超时 reaper + 额度归还、Idempotency-Key 落地 |
| Web | 修 `--panel`/`--soft`/`--primary` 三个未声明变量（账单/复盘页现在就是坏的）、删 `limit: 10000`、删死代码 |
| App | P0-4 正式签名、P0-5 签名指纹校验、P0-6 主通道切 HTTPS、`allowBackup=false` |
| 运维 | 轮换全部凭据、SSH 改密钥登录、配置采集 systemd timer |

**验收：** 20 并发 reserve 额度账面精确；路径穿越 payload 返回 403；release 包用正式证书且能装能升；服务器无明文 HTTP 主通道。

### 阶段 1：可信度（第 2-5 周）

| 端 | 任务 |
| --- | --- |
| 后端 | 幻觉检测器（数字/实体白名单）、`career_facts` 接入生成链路、`progress_steps` 真实回写、AI 痕迹真实检测、评分地板修正、prompt 抽 YAML、退避加 jitter、多图 JD 并发、OCR 超时、`.doc` 与扫描 PDF 解析补齐、导出三路径字体统一、Playwright browser pool |
| Web | 原文/改写 diff 双栏高亮界面、生成进度真实步骤展示 + 取消、额度预扣确认态 |
| App | 同步 diff 展示与真实进度、接 Crashlytics |
| 工程 | 建 `tests/`，先覆盖额度/注册/越权/幻觉四类；建三条 CI 流水线 |

**验收：** 构造一份含虚构数字的 AI 输出能被标记并在两端高亮；用户否认的经历不再出现在生成结果；进度条与后端真实阶段一致；CI 全绿。

### 阶段 2：前端与 App 体验重构（第 4-10 周，与阶段 1 部分并行）

| 端 | 任务 |
| --- | --- |
| Web | Vite + ES modules 落地；token 体系统一到 oklch 并消除硬编码色值；断点收敛为 3 个；岗位适配页改 4 步 wizard；筛选栏改 bottom sheet；dialog safe-area；i18n 双套合并 + 文案全量抽 key；a11y 逐条修 + axe 进 CI；虚拟滚动 |
| App | 启动 splash、BackHandler、edge-to-edge、键盘 insets、无障碍 semantics、矢量图标、WebView 加固、删死代码与 .bak |
| 后端 | 拆分 `main.py` 为 `api/` + `services/` + `repositories/`（按领域逐个搬，每个领域配契约测试）；依赖注入替换全局单例 |

**验收：** 首屏 JS gzip 后 < 60KB；Lighthouse 移动端性能与可访问性均 ≥ 90；axe 零 critical；`main.py` < 200 行；`async def` 路由内零裸 `store.` 调用。

### 阶段 3：数据层与推荐 1.5（第 8-16 周）

| 端 | 任务 |
| --- | --- |
| 数据 | 用户/额度/账本/订单/任务/投递迁 PG 真表 + 唯一约束 + 索引；连接池；额度改 SQL 原子更新（替换阶段 0 的乐观锁）；全部列表接口加 cursor 分页；管理统计改聚合 SQL + 缓存 |
| 雷达 | 假数据源选项下线；薪资/经验/学历归一化；title+company 软去重；过期清理 force 参数；FTS5 索引替代全扫；分页 session 缓存；结构化画像；负反馈降权；公司名归一化匹配；语义缓存 TTL + 并发；曝光漏斗统计接口 |
| 采集 | 请求间隔与 UA 统一；robots 校验；feed 白名单；健康检查告警接入 webhook；导入批量上限 |

**验收：** 单次推荐请求 P95 < 300ms；翻页无重复；`admin_stats` 单次请求对象存储调用数从万级降到个位数；采集失败 30 分钟内告警。

### 阶段 4：推荐 2.0 与规模化（第 16 周起）

Embedding + ANN 召回（pgvector 或 sqlite-vec）；行为协同过滤；A/B 框架；数据源扩展；SSE 流式生成；雷达数据迁 PG；按需评估 iOS（若做，考虑 KMP 复用 ViewModel）。

---

## 10. 云端与本地源码同步机制

当前是"本地改完手动传服务器"的模式，重构期间每天多次改动，必须先把同步机制立起来，否则会出现云端与本地不一致、改动被覆盖的事故。

1. **单一事实源在 Git。** 本地仓库为准，服务器 `/opt/jd-resume-ai` 只做 `git pull` 的消费端，禁止在服务器上直接编辑代码。若服务器当前已有未提交的本地改动（**很可能有**，需先核查 `git status`），先把它们拉回本地合并，再建立单向流。
2. **分支策略。** `main` 对应生产，`dev` 为集成分支，每项改动走短生命周期 feature 分支。阶段 0 的 P0 修复走 `hotfix/*` 直接进 `main`。
3. **部署方式。** `deploy.sh` 增强为：`git fetch && git checkout <tag> && pip install -r` → 运行 migration → `systemctl restart` → 跑 `scripts/self_test.py` 冒烟 → 失败自动回滚到上一个 tag。每次发布打 tag，App 版本号与后端 tag 关联。
4. **配置隔离。** `.env` 永远只在服务器上，不入库（现在已在 `.gitignore`，保持）。新增配置项时同步更新 `.env.example`。
5. **数据库迁移。** 引入 Alembic，migration 脚本入库，`deploy.sh` 中自动执行，禁止手改线上表结构。
6. **同步校验。** 每次部署后比对本地与服务器的 `git rev-parse HEAD`，不一致告警。

**待服务器可访问后需立即核查的项：** 服务器当前 git HEAD 与本地是否一致、是否有未提交改动、Nginx 配置与 TLS 证书状态、systemd 服务与 worker 是否都在跑、`TASK_QUEUE_ENABLED` 实际值、PostgreSQL 是否真的启用（`database_url` 是否配了）、真实数据量（用户数/岗位数/生成数）、磁盘与内存余量、日志中的错误分布、`.env` 中凭据是否仍是默认值。这些会直接影响阶段 0 与阶段 3 的排期。

---

## 11. 本次审计的边界

必须说清楚哪些结论是确证的、哪些还没验。

**已逐行回查代码确认：** 额度三函数无锁（`main.py:592-642`）、注册 check-then-act（1276-1300）、文件越权 `in` 判断（2575）、debug keystore 签名（`build.gradle.kts:25-40`）、明文 HTTP 主通道（16 行）、`--panel`/`--soft`/`--primary` 三变量未声明（`styles.css:436/1264/1268`）、AI 痕迹硬编码通过（`services.py:558`）、`progress_steps` 只在创建时写入（`main.py:2311/2419`）且前端照原样渲染（`app.js:1187`）、`app.js:747` 的 `limit: "10000"`、`_salary_min` 正则缺"万"与小数处理（`radar.py:84-96`）。

**修正了初判：** ① Android 是原生 Compose，不是 WebView 套壳；② `_salary_min` 对 "10-20K" 和 "8000-15000元/月" 实际处理正确，真正的 bug 在"1.5万"、"15万/年"这类表述。

**尚未验证（需服务器访问）：** 运行时配置、Nginx/TLS、systemd 与 worker 实况、真实数据规模、日志错误分布、TOS 实际调用量与费用、`.env` 凭据状态、服务器端是否存在未同步的本地改动。**阶段 0 开工前应先补这一轮核查**，因为它可能改变优先级排序（例如若线上还没有真实用户，额度竞态的紧急度可以后置，前端体验重构可以提前）。

**主观判断部分：** "推荐系统本质是带权重的布尔搜索"、"引入 Preact 而非 React 整体重写"、"Android 继续原生不切跨平台"这三条是权衡建议，不是代码事实，可以讨论。
